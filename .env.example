DEEPSEEK_API_KEY=sk-xxx

# 本地 MySQL（MCP connect_mysql_instance 使用，勿提交真实密码到 Git）
MYSQL_HOST=127.0.0.1
MYSQL_PORT=3306
MYSQL_USER=root
MYSQL_PASSWORD=your_password_here
MYSQL_DATABASE=test

# RAG：默认 TF-IDF 检索 slowlog/docs；设为 mock 则固定返回 2 条（eval 用）
# SLOWLOG_RAG=mock
# SLOWLOG_RAG_TOPK=3
